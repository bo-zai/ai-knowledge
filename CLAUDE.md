## 代码规范

### 命名

- 目录名使用小写英文
- TypeScript 文件使用 `kebab-case`
- 变量和函数使用 `camelCase`
- 类型、接口、schema 常量使用 `PascalCase`
- 模块级常量使用 `UPPER_SNAKE_CASE`
- 布尔变量/函数使用 `is*`、`has*`、`should*`、`can*` 前缀
- 回调函数参数命名避免 `cb`、`callback`，使用语义化名称如 `onSuccess`、`handleError`
- 避免缩写，除非是广泛认知的（如 `id`、`url`、`api`）

### 类型与复杂度

- 项目必须保持 `strict` TypeScript
- 禁止无约束 `any`，必要时使用 `unknown` 并显式类型守卫
- 函数参数建议不超过 `7`，超过时提取为参数对象
- 圈复杂度建议不超过 `15`
- 重复字面量优先提取为常量或小型 helper
- 类型定义优先使用 `interface`，需要联合/映射/递归时使用 `type`
- 避免类型断言，优先通过代码结构保证类型正确

### 注释

#### 基本原则

- 注释统一使用简体中文
- 注释只写"为什么"（意图、约束、背景），不写"怎么做"或"不做什么"
- 不写显而易见的注释，好代码应自解释

#### 内容规则

- 避免描述对比方案或历史逻辑（如"而非 X"、"不再使用 Y"），这类注释随代码变化必然过时
- 副作用函数应注明：修改全局状态、发送网络请求、写入文件等
- 安全边界和性能敏感代码应解释意图和瓶颈来源
- 复杂算法或业务规则应说明来源或给出参考链接
- 外部依赖或前提条件应注明（如"依赖 X 已初始化"）

#### 禁止事项

- 禁止注释掉的代码，git 已保存历史，应直接删除
- 禁止提交临时调试注释（debugger、临时的 // xxx）
- 禁止重复类型信息的文档注释（如 `@param {string} name - 名字`）

#### 维护规则

- 修改代码逻辑时，必须同步检查并更新相关注释
- 删除代码时，必须同时删除相关注释
- `TODO`、`FIXME`、`HACK` 使用标准前缀，并附 issue 链接或日期以便追溯

#### 特殊场景

- 魔法数字优先提取为命名常量，其次才用注释说明含义
- 公共 API 文档注释应描述参数用途而非类型，类型已由 TypeScript 声明
- 测试代码通常不需要注释，测试名应自解释意图，特殊情况才注释

### 文件与目录

#### 文件命名

- 文件名应反映职责而非实现（如 `user-validator.ts` 而非 `check-user.ts`）
- 测试文件与源文件同名加 `.test` 后缀（如 `user-validator.test.ts`）
- 类型定义集中在 `types.ts` 或 `*.types.ts`，避免分散在各文件
- 配置文件使用 `config.ts` 或 `*.config.ts`
- 工具函数使用 `utils.ts` 或 `helpers.ts`（helpers 通常带业务语义）
- 单文件模块入口为 `index.ts`，多文件模块必须有 `index.ts` 作为导出聚合

#### 目录划分

- 目录按功能/领域划分，而非按技术类型（避免 `utils/`、`services/`、`helpers/` 等通用分类）
- 目录深度建议不超过 `4` 层，超过时考虑拆分或合并
- 每个功能目录应包含完整的实现、类型、测试，避免跨目录查找
- 共享代码放在 `shared/` 或 `common/`，但需明确共享范围和职责
- 目录名使用 `kebab-case`，与文件命名一致
- 避免目录名重复文件名（如 `user/user.ts`，应改为 `user/index.ts`）

#### 文件大小与拆分

- TypeScript 文件建议不超过 **800 行**（含注释和空行），纯逻辑代码不超过 **500-600 行**
- React/Vue 组件文件建议不超过 **300-500 行**，超过时拆分为子组件
- 类型定义文件（`types.ts`）按领域拆分，避免单个类型文件过大
- 单文件应只包含 **一个核心类/模块或一组紧密相关的函数/组件**

拆分时机：
- 文件行数接近上限（超过 600 行时预警）
- 文件包含多个松耦合的类/函数/组件
- 文件承担多种职责
- 新增功能与现有代码职责边界清晰

拆分示例：

```text
# 拆分前：user-service.ts（900行，混杂多种职责）
class UserService         # 用户业务逻辑
class UserValidator       # 用户校验
class UserRepository       # 数据访问
function formatUser()     # 数据格式化

# 拆分后：
user/
├── service.ts       # UserService 核心业务（~300行）
├── validator.ts     # UserValidator 校验逻辑（~150行）
├── repository.ts    # UserRepository 数据访问（~200行）
├── formatter.ts     # 数据格式化（~100行）
├── types.ts         # 用户相关类型定义
└── index.ts         # 模块导出聚合
```

### 解耦与抽象

#### 解耦原则

- 模块间依赖必须单向，禁止循环依赖
- 模块只暴露必要接口，内部实现不对外可见
- 依赖通过接口/类型而非具体实现，便于替换和测试
- 配置和常量外置，避免在模块内硬编码依赖路径或参数
- 跨模块通信优先使用事件/回调，而非直接调用内部方法

#### 抽象原则

- 同一文件/函数内抽象层次必须一致（高层逻辑不混杂底层细节）
- 不为未来可能的需求提前抽象（YAGNI），当前需求是唯一依据
- 抽象必须有明确的接口定义和职责边界
- 抽象泄漏时优先修复抽象，而非在调用方添加特殊处理
- 三个以上相似实现才考虑抽象，两个时保持独立更灵活

### 项目适配边界

#### 核心原则

- 禁止为任何验证项目、示例项目或当前工作区中的特定仓库编写定制逻辑，所有设计与实现必须对同类项目普适成立

#### 禁止事项

- 禁止硬编码项目名、路径、模块名、包名、表名前缀、业务关键词，仅用于提升某些验证仓库效果
- 禁止根据特定仓库的目录结构、表名风格或历史结果添加例外分支、特殊阈值、白名单或黑名单
- 禁止因为某个验证项目结果不理想，就直接补项目专用兼容代码

#### 允许事项

- 允许抽象通用启发式、通用评分机制、通用证据模型
- 允许使用验证项目做回检，但回检仅用于暴露通用问题，不能反向生成项目特化逻辑
- 允许保留跨行业通用规则，但必须能说明适用边界，且不能绑定单一仓库

#### 实现自检

- 提交前必须自查新增逻辑是否包含特定项目名、路径、模块名、表名、包名或业务词硬编码；若存在，必须证明其为跨项目通用规则，否则不得提交

### 函数设计

#### 单一职责

- 一个函数只做一件事，避免"做 X 并顺便做 Y"
- 函数名应准确描述其唯一职责，名称过长说明职责过多
- 函数体超过 50 行时考虑拆分

#### 参数设计

- 参数顺序：必选参数 → 可选参数 → 配置对象 → 回调函数
- 可选参数优先使用默认值而非 `undefined` 检查
- 多个相关参数提取为参数对象（如 `{ width, height }` → `dimensions`）
- 回调函数参数命名遵循事件风格（`onSuccess`、`onError`）

#### 返回值

- 返回值类型应明确，避免 `undefined | null` 混用
- 异步函数统一返回 `Promise`，不混用回调
- 失败时抛出异常而非返回 `null`，让调用方显式处理
- 纯函数优先，副作用函数应注明

### 依赖管理

#### 导入规范

- 导入分组顺序：标准库 → 外部依赖 → 内部模块 → 相对路径 → 类型导入
- 同一来源的多个导入合并为一条语句
- 禁止相对路径超过两级（`../../`），超过时考虑目录结构调整
- 类型导入使用 `import type`，避免编译后残留

#### 依赖约束

- 禁止循环依赖，检测到时必须重构
- 外部依赖版本锁定，避免 `*` 或范围版本
- 新增外部依赖需评估体积、维护状态、兼容性
- 内部模块依赖遵循"上层依赖下层，下层不依赖上层"

### 错误处理

#### 异常类型

- 业务错误使用自定义 `Error` 子类，携带错误码和上下文
- 系统错误（网络、文件等）应捕获并转换为业务错误
- 验证错误使用 `ValidationError`，包含字段级错误信息

#### 处理策略

- 早返回：条件不满足时立即返回或抛出，避免深层嵌套
- 错误优先：`if (!condition) throw ...` 优于正向条件包裹整个逻辑
- 异常不静默吞掉，必须处理或重新抛出
- finally 块用于资源清理，不用于业务逻辑
- Promise 拒绝必须处理，避免未捕获异常

#### 错误边界

- 入口层（API 边界、CLI 边界）捕获所有异常，转换为用户友好消息
- 业务层只抛出业务异常，不捕获系统异常
- 工具层优先返回结果对象（如 `{ success, data, error }`），让调用方决定处理方式

### 代码组织

#### 文件内顺序

- 文件顶部：类型定义 → 常量 → 导入（如需类型导入）
- 导出函数/类按调用顺序自上而下排列
- 私有函数紧跟在调用它的公开函数之后
- 测试文件中测试用例按源代码函数顺序排列

#### 导出规范

- 优先使用命名导出，避免默认导出（便于 IDE 自动导入和重命名）
- `index.ts` 只导出公开接口，不导出内部实现
- 类型导出与实现导出分离（`export type { ... }` vs `export { ... }`）

#### 代码分组

- 相关代码聚集，不相关代码用空行分隔
- 函数间用单空行，逻辑块间用双空行
- 避免过多空行，紧凑但不拥挤
