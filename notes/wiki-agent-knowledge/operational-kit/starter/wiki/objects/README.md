# Objects Directory

建议目录：

```text
objects/
├── terms/
├── capabilities/
├── systems/
├── ownership/
├── contracts/
├── modules/
├── validation/
├── decisions/
├── invariants/
├── states/
└── open/
```

规则：

- 一个文件只表达一个对象
- 文件名建议与对象 ID 一致
- 每个对象必须可被独立引用

