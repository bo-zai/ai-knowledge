```
第一阶段：需求理解——我遇到了什么困难？

  拿到需求后，我脑子里立刻冒出一堆问题：

  - "打分"在这个仓库里是什么意思？ 是打分评价？还是批改？代码里叫什么？Score？Evaluate？Grade？Mark？
  - "课件类型"是什么？ 有几种？不同课件类型的打分逻辑一样吗？
  - "学习记录"是什么？ 和"练习记录"是同一个东西吗？
  - "积分发放"是什么？ 已有的积分机制在哪里？
  - "批量"意味着什么？ 已有单个打分的逻辑吗？批量化需要改什么？

  我现在要做的事：从需求文档中的业务术语，映射到代码中的具体位置。

  没有知识库时我会怎么做：

  1. grep -r "打分" --include="*.java"         → 可能 0 结果（代码用英文）
  2. grep -r "Score|Evaluate|Grade|Mark"       → 几百个结果，哪个是对的？
  3. CodeGraph search "Score"                  → 找到 ScoreDO, ScoreService, ScoreRecord...
                                                但哪个是"打分评价"？哪个是"分数记录"？
  4. grep -r "课件" --include="*.java"          → 可能 0 结果
  5. CodeGraph search "Courseware"             → 找到 CoursewareDO, CoursewareTypeEnum
                                                但"课件类型"有两种，含义不同——我不知道
  6. 读 CoursewareTypeEnum 源码                → 看到 type=3 是 PRACTICE_MUSIC
                                                但"其他类型走不同处理路径"——我不知道
  7. 读 CoursewareDO 源码                      → 看到 coursewareType 字段
                                                但这个字段影响什么？我要继续追
  8. grep "coursewareType" 看谁在用            → 十几个文件引用了这个字段
  9. 逐个读引用处                              → 花了 5 分钟，才发现 type=3 走 TeachCategoryCourse，
                                                其他走 TeachCategoryContentCourse

  这 9 步花了我大约 8~10 分钟，才搞清楚"课件类型"这一个概念。

  如果有知识库，理想情况下的路径：

  1. 读 concepts/_glossary.md → 30 秒内匹配到：
     "课件类型" → coursewareType，详情见 concepts/courseware-type.md
     "学习记录" → LearningRecordDO，详情见 concepts/learning-record.md
     "积分" → 关联到 "运营管理" 能力域

  2. 读 concepts/courseware-type.md → 1 分钟内理解：
     两种课件类型走不同数据源，type=3 走 TeachCategoryCourse...

  3. 读 index.md 业务域导航 → 看到"教学管理"域关联了课件、打分、积分

  4. 读 workflows/batch-scoring-flow.md（如果存在的话）→ 理解全流程

  对比：知识库路径 2~3 分钟 vs 自行探索 8~10 分钟。但更重要的是——自行探索时我可能找错了方向（比如把 ScoreRecord
  当成"打分评价"，实际上是"分数记录"），走弯路后花的时间更多。

  ---
  第二阶段：定位代码——我需要什么？

  理解了术语后，我要找到需要修改的代码。

  我会做的事：

  1. CodeGraph search "打分" 相关的 Controller
     → 找到 TeachController, ScoreController, RecordController
     → 但我不知道它们属于哪个业务域，不知道哪个是主入口

  2. CodeGraph context "打分功能"
     → 返回很多符号，但我需要判断哪些是核心、哪些是辅助

  3. 读 TeachController 源码
     → 发现 20 个方法，哪些和"打分"有关？
     → 逐个看方法名和注解，花 3 分钟筛选

  4. 追踪打分方法的调用链
     → CodeGraph callees teachScore()
     → 发现调了 ScoreService → LearningRecordService → OperationService
     → 但这条链的业务含义是什么？"打分 → 更新学习记录 → 发积分"？
     → 我不确定，需要继续读代码确认

  这里我真正需要的是一张"业务地图"——告诉我"打分"属于"教学管理"域，入口在
  TeachController.score()，下游影响学习记录和积分。有了这张地图，我直接去读正确的文件，而不是在一片符号中摸索。

  ---
  第三阶段：理解影响范围——我最容易出错的地方

  假设我要新增"批量打分"功能，我需要知道：

  1. 现有的单个打分流程经过了哪些步骤？
     → 我要自己追踪调用链，深度 3~4 层
     → 中间可能有异步调用（@EventListener）→ CodeGraph trace 可能断在这里
     → 我可能漏掉异步触发的积分发放

  2. 打分有哪些约束？
     → grep "throw" in ScoreService → 5 个 throw
     → 但哪些是业务约束？哪些是参数校验？
     → 我可能把 null check 也当业务约束
     → 我可能漏掉"只有教师角色可以打分"这个权限约束（在 Controller 层的注解里）

  3. 批量操作有什么特殊考虑？
     → 现有代码有没有批量操作的模式可以参考？
     → 事务边界怎么处理？一个失败全部回滚还是部分成功？
     → 这些我都需要从已有代码中推断

  这个阶段是我最容易遗漏信息的地方。 如果预生成的知识告诉我"打分触发积分发放（异步）"、"只有教师角色可以打分"、"打分使用乐观锁保护并发"，我就不会漏掉这些。

  ---
  第四阶段：编码实现——我需要什么指导？

  开始写代码了：

  1. 新的 Controller 方法放哪？
     → 我需要知道分包模式（按层还是按领域）
     → 读现有 Controller 的包路径推断 → 1 分钟

  2. 新的 Service 方法放哪？
     → 同上，看现有 Service 的包路径 → 1 分钟

  3. 遵循什么命名约定？
     → 看现有方法名推断 → 30 秒

  4. 事务怎么处理？
     → 看现有 Service 的 @Transactional 使用模式 → 2 分钟

  这个阶段我基本不需要预生成知识。 代码模式我通过读现有代码就能学会。架构概览中的"分包模式"和"编码约定"有一点用，但我花 1~2 分钟看现有代码也能推断出来。

  ---
  基于以上模拟的分析

  🔴 必须预生成（真实帮到 Agent 的知识）

  1. 概念知识（concepts/）—— 最高价值

  模拟中最大的时间黑洞就是"理解业务术语"。Agent 拿到需求后，第一件事就是把需求文档中的术语映射到代码。没有知识库时，这个映射过程是试错式的——猜一个英文名 →
  搜索 → 不对 → 换一个 → 再搜。有了概念知识 + 术语速查表，映射变成查表式的。

  2. 跨域业务流程（workflows/）—— 高价值

  模拟第三阶段中，我最怕的就是"漏掉链路上的某个环节"。比如打分后异步触发积分——CodeGraph trace 在 @EventListener
  处断了，我就不知道后面还有积分发放。预生成的端到端流程直接告诉我完整路径和关键分支，避免遗漏。

  3. 约束知识（constraints/）—— 高价值

  模拟第三阶段中，我需要知道"哪些规则不能违反"。grep throw
  能找到原始数据，但我分不清哪些是业务约束、哪些是工程惯例。预生成的约束知识帮我过滤掉了噪音，只留下真正需要遵守的业务规则。

  4. 能力目录的域级信息 —— 中高价值

  注意，我说的是域级信息（域名称、域描述、域级业务上下文），不是操作列表。模拟第二阶段中，我需要的是"打分属于教学管理域"这样的业务地图，而不是"TeachControll
  er 有 20 个方法"这样的清单。域级信息帮我缩小搜索范围，操作清单我自己看代码就行。

  🟡 有一定价值但不必须

  5. 边界知识（boundaries/）

  偶尔有用（比如"短信校验已禁用"这种信息能避免我踩坑），但大多数场景下不影响实现。条目数量少，生成成本低，保留无害。

  6. 外部系统交互（external-systems/）

  只在需求涉及外部系统时才有用。大多数需求不涉及外部系统交互，这时这些知识就是死代码。但涉及时的价值很高（比如"微信支付学生端和教师端用不同 AppId"）。

  7. 数据模型的聚合边界（data-model/）

  聚合边界对理解"修改一个实体时还要改哪些关联实体"有用。但这个信息我通过 CodeGraph impact 分析也能大致得到。

  🟢 不需要预生成（Agent 自己搞定更好）

  8. 能力关系的调用链 —— 不需要

  这是我要修正之前分析的重点。CodeGraph 的 trace、callers、callees、impact 做的是同一件事，而且更好：
  - 预生成的能力关系是静态快照，代码改了就可能过时
  - CodeGraph 给出的是实时数据，永远和代码同步
  - Agent 需要理解调用链的业务含义时，可以边 trace 边读代码，比读预生成的文字描述更准确

  预生成能力关系唯一的价值是"业务语义化命名"（把 A→B→C 命名为"课表制定触发积分发放"），但这个价值不足以支撑一个独立的知识类型。

  9. 能力目录的操作列表 —— 不需要

  Agent 用 CodeGraph explore 一个 Controller，1
  秒内就能看到所有方法签名、注解、参数。预生成的操作列表只是把代码翻译成了表格，没有信息增量。域级业务上下文有价值（上面已列为 🔴），但操作清单没有。

  10. 架构概览的大部分字段 —— 不需要

  - 项目类型 → 读 pom.xml（1 次调用）
  - 技术栈 → 读 pom.xml（同上）
  - 目录结构 → ls（1 次调用）
  - 忽略目录 → 工程惯例（Agent 自带）
  - 启动类 → CodeGraph search（1 次调用）

  唯一有用的字段是"分包模式"（按层/按领域/混合）—— 这告诉我新代码放哪。但这是一个 1 句话的结论，不需要一整篇 architecture.md 来承载。

  11. 索引体系（index.md / _index.md）—— 看情况

  如果知识库条目少于 20 个，Agent 直接 Glob 扫目录就行。如果条目很多（50+），索引有导航价值。但这取决于项目规模，不是所有项目都需要。

  ---
  重新审视设计文档的核心假设

  设计文档有一个隐含假设：Agent 会在收到需求后先读知识库，再开始工作。

  但实际不是这样的。Agent 的真实工作模式是：

  收到需求 → 尝试理解术语 → 搜索代码 → 读代码 → 形成理解 → 编码 → 测试
                  ↑                              ↑
            知识库在这里有用              知识库在这里有用
           （术语映射到代码）          （约束和流程防遗漏）

  Agent 不会"先读完知识库再工作"。Agent 是在工作过程中按需查阅知识。这意味着：

  1. 知识库必须是可碎片化使用的——每个文件独立可理解，不需要先读 index.md
  2. 文件名和标签必须高度可预测——Agent 要能从文件名判断"这个文件对我当前任务有没有用"
  3. 信息密度必须高——Agent 加载一个知识文件花费 token 预算，如果信息密度低（比如操作列表只是代码翻译），不如不加载

  ---
  最终结论：如果我是 Agent，我希望知识库里有什么

  ┌────────┬─────────────────────┬────────────────────────────────────────────────┬──────────────────────────────┐
  │ 优先级 │      知识类型       │               我希望它告诉我什么               │        我不需要的部分        │
  ├────────┼─────────────────────┼────────────────────────────────────────────────┼──────────────────────────────┤
  │   1    │ 概念知识 + 术语速查 │ 需求中的术语在代码里叫什么、什么意思、有什么坑 │ 简单枚举的值列表             │
  ├────────┼─────────────────────┼────────────────────────────────────────────────┼──────────────────────────────┤
  │   2    │ 跨域业务流程        │ 端到端路径、关键分支、异步触发点               │ 单域内的流程（我自己 trace） │
  ├────────┼─────────────────────┼────────────────────────────────────────────────┼──────────────────────────────┤
  │   3    │ 约束知识            │ 业务规则（不是工程惯例）、作用范围、违反后果   │ null check 之类的通用校验    │
  ├────────┼─────────────────────┼────────────────────────────────────────────────┼──────────────────────────────┤
  │   4    │ 能力域地图          │ 业务域列表 + 域级上下文 + 入口在哪             │ 每个域的操作清单             │
  ├────────┼─────────────────────┼────────────────────────────────────────────────┼──────────────────────────────┤
  │   5    │ 边界知识            │ 禁用功能、能力局限                             │ —                            │
  ├────────┼─────────────────────┼────────────────────────────────────────────────┼──────────────────────────────┤
  │   6    │ 分包模式            │ 一句话：新代码放哪                             │ 完整的技术栈/目录结构/启动类 │
  ├────────┼─────────────────────┼────────────────────────────────────────────────┼──────────────────────────────┤
  │   —    │ ~~能力关系~~        │ CodeGraph 做得更好                             │ 全部                         │
  ├────────┼─────────────────────┼────────────────────────────────────────────────┼──────────────────────────────┤
  │   —    │ ~~数据模型聚合~~    │ CodeGraph impact 大致可替代                    │ 大部分                       │
  ├────────┼─────────────────────┼────────────────────────────────────────────────┼──────────────────────────────┤
  │   —    │ ~~操作列表~~        │ 代码本身就是清单                               │ 全部                         │
  ├────────┼─────────────────────┼────────────────────────────────────────────────┼──────────────────────────────┤
  │   —    │ ~~完整架构概览~~    │ 只需分包模式一句话                             │ 其他字段                     │
  └────────┴─────────────────────┴────────────────────────────────────────────────┴──────────────────────────────┘

  一句话总结：Agent
  真正需要的是代码告诉不了的业务理解（术语含义、跨域流程、业务约束、域地图），而不是代码本身的结构信息（调用关系、操作列表、实体字段、技术栈）——后者 Agent
  用 CodeGraph 实时查更好。
```
